import { DatepicketDirective } from './datepicket.directive';

describe('DatepicketDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepicketDirective();
    expect(directive).toBeTruthy();
  });
});
